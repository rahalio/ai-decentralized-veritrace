/**
 * Adapters DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: adapters
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Adapters Domain Module - Composition root
 *
 * DDD: Composition root for adapters domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  LbrEndpointRepositoryAdapter,
  LbrLinkageRepositoryAdapter,
  LedgerAdapterRepositoryAdapter,
} from "@veritrace/adapters/adapters";
import { getIdGeneratorService } from "@veritrace/adapters";
import type { AdapterDynamoDBClient } from "@veritrace/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateLbrEndpoint,
  ExecuteCreateLedgerAdapter,
  ExecuteGetLbrEndpoint,
  ExecuteGetLearnerLbrLinkage,
  ExecuteGetLedgerAdapter,
  ExecuteListLbrEndpoints,
  ExecuteListLedgerAdapters,
  ExecuteSetLearnerLbrLinkage,
  ExecuteUpdateLbrEndpoint,
} from "@veritrace/services/adapters/usecases";
// Ports (interfaces)
import type {
  LbrEndpointRepository,
  LbrLinkageRepository,
  LedgerAdapterRepository,
} from "@veritrace/services/adapters/ports";

export interface AdaptersDomainModule {
  repos: {
    lbrEndpoints: LbrEndpointRepository;
    lbrLinkages: LbrLinkageRepository;
    ledgerAdapters: LedgerAdapterRepository;
  };
  useCases: {
    lbrEndpoints: {
      create: ExecuteCreateLbrEndpoint;
      get: ExecuteGetLbrEndpoint;
      list: ExecuteListLbrEndpoints;
      update: ExecuteUpdateLbrEndpoint;
    };
    lbrLinkages: {
      get: ExecuteGetLearnerLbrLinkage;
      update: ExecuteSetLearnerLbrLinkage;
    };
    ledgerAdapters: {
      create: ExecuteCreateLedgerAdapter;
      get: ExecuteGetLedgerAdapter;
      list: ExecuteListLedgerAdapters;
    };
  };
}

export function buildAdaptersDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): AdaptersDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      lbrEndpoints?: LbrEndpointRepository;
      lbrLinkages?: LbrLinkageRepository;
      ledgerAdapters?: LedgerAdapterRepository;
    } = {};

    obj.lbrEndpoints = new LbrEndpointRepositoryAdapter(dynamoClient);
    obj.lbrLinkages = new LbrLinkageRepositoryAdapter(dynamoClient);
    obj.ledgerAdapters = new LedgerAdapterRepositoryAdapter(dynamoClient);
    return {
      lbrEndpoints: obj.lbrEndpoints!,
      lbrLinkages: obj.lbrLinkages!,
      ledgerAdapters: obj.ledgerAdapters!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    lbrEndpoints: {
      create: new ExecuteCreateLbrEndpoint(executionContext, idGenerator, repos.lbrEndpoints),
      get: new ExecuteGetLbrEndpoint(executionContext, idGenerator, repos.lbrEndpoints),
      list: new ExecuteListLbrEndpoints(executionContext, idGenerator, repos.lbrEndpoints),
      update: new ExecuteUpdateLbrEndpoint(executionContext, idGenerator, repos.lbrEndpoints),
    },
    lbrLinkages: {
      get: new ExecuteGetLearnerLbrLinkage(executionContext, idGenerator, repos.lbrLinkages),
      update: new ExecuteSetLearnerLbrLinkage(executionContext, idGenerator, repos.lbrLinkages),
    },
    ledgerAdapters: {
      create: new ExecuteCreateLedgerAdapter(executionContext, idGenerator, repos.ledgerAdapters),
      get: new ExecuteGetLedgerAdapter(executionContext, idGenerator, repos.ledgerAdapters),
      list: new ExecuteListLedgerAdapters(executionContext, idGenerator, repos.ledgerAdapters),
    },
  };
  return { repos, useCases: useCases as AdaptersDomainModule["useCases"] };
}
