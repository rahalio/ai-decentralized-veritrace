/**
 * Verification Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: verifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import type { VerificationEntity } from "@veritrace/core/verifications/models/index.js";
import { verificationsSchemas } from "@veritrace/core/verifications";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "vrf";

export function createVerification(overrides: Partial<VerificationEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    blockId: overrides.blockId ?? `blk_${ulid().toLowerCase()}`,
    status: overrides.status ?? "valid",
    verificationId: overrides.verificationId ?? `vrf_${ulid().toLowerCase()}`,
    verifiedAt: overrides.verifiedAt ?? new Date().toISOString(),
    ...overrides,
  };

  return verificationsSchemas.Verification.parse(raw);
}

export function verificationBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createVerification(overrides));
}
