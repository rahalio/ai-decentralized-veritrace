/**
 * GetNecessityDecision handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: spaces
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getNecessityDecision } from "@veritrace/api-server/domains/spaces/handlers";
import type { SpacesDDDDependencies } from "@veritrace/api-server/domains/spaces";
import { createNecessityDecision as createNecessityDecisionEntity } from "@veritrace/factories/spaces";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veritrace/tests/shared";
import { SPACES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetNecessityDecision", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SpacesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SpacesDDDDependencies>(
      SPACES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createNecessityDecisionEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetNecessityDecision, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { necessityDecisionId: entity.id };

    await getNecessityDecision(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetNecessityDecision.execute).toHaveBeenCalledWith(expect.objectContaining({ necessityDecisionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { necessityDecisionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetNecessityDecision, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getNecessityDecision(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
