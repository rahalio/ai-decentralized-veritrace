/**
 * GetResearchExport handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: exports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getResearchExport } from "@veritrace/api-server/domains/exports/handlers";
import type { ExportsDDDDependencies } from "@veritrace/api-server/domains/exports";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veritrace/tests/shared";
import { EXPORTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetResearchExport", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ExportsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ExportsDDDDependencies>(
      EXPORTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetResearchExport, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { exportId: entity.id };

    await getResearchExport(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetResearchExport.execute).toHaveBeenCalledWith(expect.objectContaining({ exportId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { exportId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetResearchExport, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getResearchExport(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
