/**
 * GetCommitment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: commitments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCommitment } from "@veritrace/api-server/domains/commitments/handlers";
import type { CommitmentsDDDDependencies } from "@veritrace/api-server/domains/commitments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veritrace/tests/shared";
import { COMMITMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCommitment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CommitmentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CommitmentsDDDDependencies>(
      COMMITMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCommitment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { commitmentId: entity.id };

    await getCommitment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCommitment.execute).toHaveBeenCalledWith(expect.objectContaining({ commitmentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { commitmentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCommitment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCommitment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
