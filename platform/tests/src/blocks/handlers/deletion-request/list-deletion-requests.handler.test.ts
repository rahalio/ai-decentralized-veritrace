/**
 * ListDeletionRequests handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: blocks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listDeletionRequests } from "@veritrace/api-server/domains/blocks/handlers";
import type { BlocksDDDDependencies } from "@veritrace/api-server/domains/blocks";
import { createDeletionRequest as createDeletionRequestEntity } from "@veritrace/factories/blocks";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veritrace/tests/shared";
import { BLOCKS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListDeletionRequests", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: BlocksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<BlocksDDDDependencies>(
      BLOCKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createDeletionRequestEntity({ orgId }),
      createDeletionRequestEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListDeletionRequests, "execute").mockResolvedValue(useCaseResult);

    await listDeletionRequests(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListDeletionRequests.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
