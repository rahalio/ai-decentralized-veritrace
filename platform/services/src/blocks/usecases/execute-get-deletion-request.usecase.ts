/**
 * ExecuteGetDeletionRequest - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: blocks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetDeletionRequest Use Case
 *
 * DDD: Application use case for get DeletionRequest.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetDeletionRequestInput, GetDeletionRequestOutput } from "../dto/deletion-request.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veritrace/services/_shared/index.js";
import type { DeletionRequestRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetDeletionRequest {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly deletionRequest: DeletionRequestRepository
  ) {}

  async execute(input: GetDeletionRequestInput): Promise<GetDeletionRequestOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.blkId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).deletionRequestId) {
      throw new ValidationError("deletionRequestId is required");
    }
    const deletionrequestId = (input as any).deletionRequestId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.deletionRequest.getDeletionRequest({ deletionRequestId: deletionrequestId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${deletionrequestId}`);
    }

    return entity as GetDeletionRequestOutput;
  }
}
