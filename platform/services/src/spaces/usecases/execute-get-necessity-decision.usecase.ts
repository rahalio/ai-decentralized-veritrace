/**
 * ExecuteGetNecessityDecision - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: spaces
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetNecessityDecision Use Case
 *
 * DDD: Application use case for get NecessityDecision.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetNecessityDecisionInput, GetNecessityDecisionOutput } from "../dto/necessity-decision.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veritrace/services/_shared/index.js";
import type { NecessityDecisionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetNecessityDecision {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly necessityDecision: NecessityDecisionRepository
  ) {}

  async execute(input: GetNecessityDecisionInput): Promise<GetNecessityDecisionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.spcId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).spaceId) {
      throw new ValidationError("spaceId is required");
    }
    const spaceId = (input as any).spaceId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.necessityDecision.getNecessityDecision({ spaceId: spaceId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${spaceId}`);
    }

    return entity as GetNecessityDecisionOutput;
  }
}
