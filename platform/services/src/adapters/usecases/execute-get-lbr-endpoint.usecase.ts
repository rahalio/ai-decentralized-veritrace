/**
 * ExecuteGetLbrEndpoint - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: adapters
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetLbrEndpoint Use Case
 *
 * DDD: Application use case for get LbrEndpoint.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetLbrEndpointInput, GetLbrEndpointOutput } from "../dto/lbr-endpoint.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veritrace/services/_shared/index.js";
import type { LbrEndpointRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetLbrEndpoint {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly lbrEndpoint: LbrEndpointRepository
  ) {}

  async execute(input: GetLbrEndpointInput): Promise<GetLbrEndpointOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.adpId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).lbrEndpointId) {
      throw new ValidationError("lbrEndpointId is required");
    }
    const lbrendpointId = (input as any).lbrEndpointId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.lbrEndpoint.getLbrEndpoint({ lbrEndpointId: lbrendpointId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${lbrendpointId}`);
    }

    return entity as GetLbrEndpointOutput;
  }
}
