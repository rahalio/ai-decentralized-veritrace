/**
 * ExecuteGetResearchExport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: exports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetResearchExport Use Case
 *
 * DDD: Application use case for get Export.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetResearchExportInput, GetResearchExportOutput } from "../dto/export.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veritrace/services/_shared/index.js";
import type { ExportRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetResearchExport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly exportRepo: ExportRepository
  ) {}

  async execute(input: GetResearchExportInput): Promise<GetResearchExportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.expId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).exportId) {
      throw new ValidationError("exportId is required");
    }
    const exportId = (input as any).exportId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.exportRepo.getResearchExport({ exportId: exportId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${exportId}`);
    }

    return entity as GetResearchExportOutput;
  }
}
