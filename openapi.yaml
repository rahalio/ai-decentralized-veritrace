# DEPRECATED — domain specs live under packages/openapi-core/src/
# One YAML per domain: identity, spaces, blocks, commitments, consents, verifications, exports, adapters.
# See PRODUCT.md and packages/openapi-core/README.md.
openapi: 3.1.0
info:
  title: Veritrace (see packages/openapi-core)
  version: 0.1.0
  description: >
    Root skeleton retired. Use packages/openapi-core/src/{domain}.yaml.
paths: {}
